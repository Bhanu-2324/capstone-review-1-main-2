How to initate the web page ::

first get in contact with the foating google form and keep that in contact,

use the streamlite so use the terminal code that initates the code and auto redirects to the web page :

streamlit run app.py

;;

